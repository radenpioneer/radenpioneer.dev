---
import type { GetImageResult } from 'astro'
import HeaderRC from './header'

export interface Props {
  image: GetImageResult
}

const { image } = Astro.props
---

<header class="container-fluid">
  <HeaderRC image={image} />
</header>

<style>
  header {
    position: fixed;
  }
</style>