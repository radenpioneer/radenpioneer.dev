---
import { getCollection } from 'astro:content'
import { Posts as Component } from './posts'

const posts = await getCollection('posts', ({ data }) =>
  import.meta.env.MODE === 'production' ? !data.draft && !data.hidden : true
)
---

<Component posts={posts} />
